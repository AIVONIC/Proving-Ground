# Disposable, local-only; the stack is deleted after grading.
#   PG_TYPEBOT_DB_PASSWORD:        any value
#   PG_TYPEBOT_ENCRYPTION_SECRET:  EXACTLY 32 characters (raw AES-256 key)
PG_TYPEBOT_DB_PASSWORD=
PG_TYPEBOT_ENCRYPTION_SECRET=
