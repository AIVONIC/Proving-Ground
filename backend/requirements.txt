# Grading engine core
httpx>=0.27
pydantic>=2.6
pyyaml>=6.0
anthropic>=0.40
python-socketio[asyncio_client]>=5.11   # Socket.IO agents (our own widget agents)
aiohttp>=3.9

# API (added in Phase 1 storage/api work)
fastapi>=0.110
uvicorn>=0.29

# Dev
pytest>=8.0
pytest-asyncio>=0.23
